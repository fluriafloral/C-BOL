// FOR

FOR (INTEGER i = 0; i < 10; i = 10)
    IF (i == 1)
        CONTINUE;
    END-IF;
    x = i;
END-FOR;

// FOR-EACH

FOR (INTEGER i : arrayname) 
    IF (i == 1)
        CONTINUE;
    END-IF;
    y = i;
END-FOR; 

// DO-THEN-WHILE

DO
    x = 73;
THEN WHILE (x != 73);